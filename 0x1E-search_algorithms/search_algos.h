#ifndef __MAIN__
#define __MAIN__
#include <stdio.h>

int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);
void print(int *array, int current_index, size_t size);

#endif /* end of __MAIN__*/
