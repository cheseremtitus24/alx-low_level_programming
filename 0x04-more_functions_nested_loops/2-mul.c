#include "main.h"
/**
 * mul - Perform Multiplication of 2 integers.
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 *
 * Description - Multiplies two digits and returns the result
 * Return: result of multiplation always
 */

int mul(int a, int b)
{
	return (a * b);
}
